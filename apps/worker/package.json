{
  "name": "@cma/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "enqueue": "tsx src/enqueueAll.ts",
    "enqueue:reports": "tsx src/enqueueDailyReports.ts",
    "scheduler": "tsx src/scheduler.ts"
  },
  "dependencies": {
    "@cma/ai": "*",
    "@cma/core": "*",
    "@cma/db": "*",
    "@cma/detection": "*",
    "@cma/extraction": "*",
    "@cma/notifications": "*",
    "@cma/queue": "*"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
