/**
 * Placeholder home page. Phase 1 deliverable is the API + data
 * foundation (see PHASE0-ANALYSIS.md / Phase 1 report) - the real
 * dashboard (Section 16-18 of the brief) is Phase 8.
 */
export default function HomePage() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: "2rem", maxWidth: 640 }}>
      <h1>Competitor Monitor AI</h1>
      <p>Phase 1 foundation. The dashboard is not built yet - the API is reachable under /api.</p>
    </main>
  );
}
